#include "main.h"

/**
 * _isupper - function name
 *
 * @c: parameter
 *
 * Return: 1 (upper), 0(ohert")
 *
 */
int _isupper(int c)
{
        if ((c >= 65) && (c <= 90))
        {
                return (1);
        }
        else
        {
                return (0);
        }
}
