#include "main.h"
#include <string.h>

/**
 *  _strlen_recursion - function to find string length
 *
 *  @s: the string
 *
 *  Return: x (Success)
 */

int _strlen_recursion(char *s)
{
	int x;

	x = strlen(s);
	return (x);
}
