#include <stdio.h>
/**
 * add - the function to perform computation
 *
 * @a: the first parameter
 *
 * @b: the seconf=d parameter
 *
 * Return: sum Always (Succes)
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
